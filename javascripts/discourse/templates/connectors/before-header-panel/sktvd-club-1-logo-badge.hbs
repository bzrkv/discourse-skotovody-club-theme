{{! "КЛУБ" badge shown right after the logo.

    The brand logo itself is the native Discourse #site-logo image (uploaded by
    the admin via Customize → Logo). We do NOT render a text wordmark here —
    only the "КЛУБ" badge that sits next to the uploaded logo.

    Connector filename (sktvd-club-1-logo-badge) deliberately differs from the
    styled class (.sktvd-logo-badge) so the outlet wrapper stays unstyled. }}
<span class="sktvd-logo-badge">Клуб</span>
